var image = document.getElementsByClassName('furn');
new simpleParallax(image, {
    scale: 1.3,
    orientation: 'left'
});
var image = document.getElementsByClassName('dec');
new simpleParallax(image, {
    scale: 1.1,
    orientation: 'right'
});